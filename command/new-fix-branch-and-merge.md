---
agent: plan
ignored: true
---
Take note of which branch we are currently in. 

!`baseone expand ~/ocs/md/edit-branch--body.md fix "$ARGUMENTS"`
After you've finished and pushed the changes to origin, return to the branch on which we'd started and then merge the changes you'd just pushed on the $1 branch back into it.