If anything about the $1 request isn't clear, you can pause to ask me questions with the `question` tool if needed. 

Once you have made your plan to implement this new branch, use your `plan_exit` tool to switch into build mode to create the new branch and proceed to implement it and push it to origin.

Once you've made your changes in the $2 branch, push them to origin.